# 🧪 Практична робота №1

## Основи роботи з Linux та файловою системою

### 🎯 Мета роботи

* Ознайомитися з терміналом Linux
* Навчитися орієнтуватися у файловій системі
* Освоїти базові команди роботи з файлами та каталогами
* Зрозуміти принципи абсолютних і відносних шляхів

---

## 1. Відкриття терміналу та перевірка системи

```bash
uname -a
```

✔ показує ядро Linux, архітектуру та версію ОС

```bash
whoami
```

✔ показує ім’я поточного користувача

```bash
pwd
```

✔ показує поточний каталог (working directory)

---

## 2. Перегляд вмісту каталогів (`ls`)

```bash
ls
```

✔ простий список файлів

```bash
ls -l
```

✔ детальний список (права, власник, розмір, дата)

```bash
ls -la
```

✔ показує **включно з прихованими файлами** (`.`)

```bash
ls /etc
```

✔ перегляд іншого каталогу без переходу в нього

---

## 3. Навігація по файловій системі (`cd`)

```bash
cd /etc
pwd
```

```bash
cd ..
pwd
```

```bash
cd ~
pwd
```

✔ `~` — домашній каталог користувача

```bash
cd -
```

✔ повернення в попередній каталог

---

## 4. Створення каталогів (`mkdir`)

```bash
mkdir test
```

```bash
mkdir labs
cd labs
```

```bash
mkdir lab1 lab2 lab3
```

```bash
mkdir -p projects/java/backend
```

✔ `-p` створює вкладені каталоги одразу

---

## 5. Створення файлів

```bash
touch file1.txt
```

```bash
touch a.txt b.txt c.txt
```

```bash
ls -l
```

---

## 6. Перегляд вмісту файлів (`cat`, `less`)

```bash
echo "Hello Linux" > hello.txt
```

```bash
cat hello.txt
```

```bash
echo "Second line" >> hello.txt
cat hello.txt
```

✔ `>` — перезапис
✔ `>>` — додавання

```bash
less /etc/passwd
```

✔ навігація: `↑ ↓`, вихід — `q`

---

## 7. Копіювання та переміщення (`cp`, `mv`)

```bash
cp hello.txt hello_copy.txt
```

```bash
mv hello_copy.txt lab1/
```

```bash
mv hello.txt hello_old.txt
```

---

## 8. Видалення файлів і каталогів (`rm`)

```bash
rm a.txt
```

```bash
rm -i b.txt
```

✔ `-i` — підтвердження

```bash
rm -r lab3
```

⚠ пояснити **чому `rm -rf /` — зло**

---

## 9. Перегляд структури каталогів (`tree`)

```bash
tree
```

Якщо немає:

```bash
sudo apt install tree
```

---

## 10. Перегляд процесів (`top`, `ps`)

```bash
top
```

✔ реальний час
✔ `q` — вихід

```bash
ps aux
```

```bash
ps aux | head
```

---

## 11. Перевірка використання диску

```bash
df -h
```

```bash
du -sh *
```

---

## 12. Пошук файлів (`find`)

```bash
find . -name "*.txt"
```

```bash
find /etc -name "*.conf" 2>/dev/null
```

✔ `2>/dev/null` — прибрати помилки доступу

---

## 13. Пошук тексту у файлах (`grep`)

```bash
grep root /etc/passwd
```

```bash
grep -r "localhost" /etc
```

---

## 14. Аналіз мережі (`netstat`, `ss`)

```bash
netstat -tulnp
```

або сучасніше:

```bash
ss -tulnp
```

✔ відкриті порти і служби

---

## 15. Перевірка мережі

```bash
ip a
```

```bash
ip route
```

```bash
ping google.com
```

---

## 16. Завдання для самостійної роботи

1. Створити каталог `linux_practice`
2. Усередині створити `lab1/files`
3. Створити 3 файли з текстом
4. Знайти всі `.txt`
5. Скопіювати один файл у інший каталог
6. Подивитись активні процеси
7. Вивести відкриті мережеві порти

---

# 📌 Результат практичної роботи

Студент:

* **не боїться терміналу**
* розуміє **як влаштована файлова система**
* реально вміє **створювати, копіювати, видаляти, шукати**
* бачить процеси та мережу

---

## Що далі

Якщо хочеш, наступним кроком я:

* оформлю це **як методичку (DOCX / PDF)**
* або **одразу зроблю Практичну №2** (користувачі, права, `chmod`, `chown`, `kill`, `htop`)
* або адаптую **під конкретний дистрибутив (Ubuntu / Debian / Rocky / Arch)**

Скажи, в якому форматі рухаємося 👍
